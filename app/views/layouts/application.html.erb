<!DOCTYPE html>
<html>
  <head>
    <title>LeafWhisper</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="flex justify-between bg-slate-950 text-neutral">
    <%= render 'shared/header' %>
    <section class="hero text-center py-20 bg-base-100">
      <div class="container mx-auto">
        <h1 class="text-6xl bg-primary">日本茶の世界へようこそ</h1>
        <p class="text-2xl text-neutral mt-4">LeafWhisperでお茶の真髄を体験してください</p>
        <a href="#learn-more" class="mt-8 inline-block bg-accent text-white py-3 px-6 rounded hover:bg-accent-focus">もっと詳しく</a>
      </div>
    </section>
    <div class="container mx-auto">
      <% if notice %>
        <p><%= notice %></p>
      <% end %>
      <% if alert %>
        <p><%= alert %></p>
      <% end %>
    </div>
    <%= yield %>
    <%= render 'shared/footer' %>
  </body>
</html>

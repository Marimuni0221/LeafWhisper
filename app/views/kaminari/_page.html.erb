<% if page.current? %>
  <span class="join-item btn bg-emerald-200 text-white px-2 sm:px-4 py-1 sm:py-2"><%= page %></span>
<% else %>
  <%= link_to page, url, remote: remote, rel: page.rel, class: "join-item btn px-2 sm:px-4 py-1 sm:py-2" %>
<% end %>